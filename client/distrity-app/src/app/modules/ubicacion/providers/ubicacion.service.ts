import { Injectable } from '@angular/core';

//import { global } from '../../shared/models/global';

//import { Direccion } from '../models/direccion';
//import { Zona } from '../models/zona';

@Injectable()
export class UbicacionService {

  //private url: string; // URL to web api

  constructor() {
/*    if (global.production) {
      this.url = `${global.productionEndpoint}/ubicaciones`;
    } else {
      this.url = `${global.developEndpoint}/ubicaciones`;
    }*/
  }
/*
  private getHeaders(sesion): Headers {
    let headers = new Headers();
    if (sesion) {
      headers.append('Authorization', 'Bearer ' + sesion.token);
      headers.append('Content-Type', 'application/json');
    }
    return headers;
  }
*/
}
